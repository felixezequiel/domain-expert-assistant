<!--
  AUTO-GENERATED by scripts/sync-rules.mjs from .claude/skills/<name>/SKILL.md.
  DO NOT EDIT THIS FILE DIRECTLY. Edit the source skill and run: npm run sync:rules
-->

# code-review

> Code review with Mermaid diagram showing execution cycle and findings positioned in flow.
> Source of truth: `.claude/skills/code-review/SKILL.md`

**Trigger:**

- Reviewing a PR that touches multiple architectural layers
- Need to visualize where findings are in the execution flow
- User asks for "/review" or "code review" with architectural analysis

**Skip when:**

- PR only touches a single file or cosmetic changes
- Review is about test-only changes with no production code

# Architectural Code Review

## Overview

A diagram is worth more than a list. The reviewer should look at the diagram and
immediately understand WHERE problems are in the execution flow.

## Flow

### Step 1: Analyze the PR

1. Fetch PR details (`gh pr view`) and complete diff (`gh pr diff`)
2. Treat the diff as the **entry point**, not the scope of review
3. Read the full files at PR HEAD (not only diff hunks) to see changes in real context
4. Trace callers and dependents of every changed symbol (functions, classes, types, exports) to assess real impact
5. Identify all findings with severity — including impacts on code **not touched** by the diff but affected by the changes

### Step 2: Map the Execution Cycle

Identify the real runtime flow with the changes applied. Include upstream callers
and downstream dependents that are part of the affected execution path, even when
not in the diff.

For this template, the typical flow is:

```
HTTP/GraphQL adapter
  → Command.of(...)
    → ApplicationService.execute(useCase, command)
      → unitOfWork.begin()
      → useCase.execute(command)
        → aggregate.create() / aggregate method → addDomainEvent()
      → drain tracked aggregates → dispatch / publish events
      → eventStore.saveAll(events)
      → unitOfWork.commit() (transactional flush)
      → sseBroadcaster.broadcastAll(events)
```

### Step 3: Build the Mermaid Diagram

Use `flowchart TD` with subgraphs per architecture layer (HTTP / Application /
Domain / Infrastructure). Solid arrows for main flow, dotted arrows for cross-cutting,
colors by severity, short description on each problem node (max 5-6 words).

### Step 4: Color by Severity

| Severity | Fill      | Stroke    | Stroke-width |
| -------- | --------- | --------- | ------------ |
| Blocking | `#d32f2f` | `#b71c1c` | 3px          |
| High     | `#e65100` | `#bf360c` | 2px          |
| Medium   | `#f9a825` | `#f57f17` | 2px          |
| Low      | `#90caf9` | `#1565c0` | 2px          |
| OK       | default   | default   | default      |

## Review Body Template

```markdown
## Code Review — PR #XX

**N blocking, N high, N medium, N low** — details in inline comments.

### Findings map — Execution cycle

(mermaid diagram)

> Blocking · High · Medium · Low

### Positive points

- item 1
```

## Behavior Rules

### MUST
- ALWAYS include Mermaid diagram in every code review
- ALWAYS map the real execution cycle (not just list files)
- ALWAYS read the full files at PR HEAD (not only the diff hunks)
- ALWAYS trace callers and dependents of changed symbols
- ALWAYS evaluate the system with the changes applied, including code paths not in the diff
- ALWAYS color nodes by severity with the defined palette
- ALWAYS include short description on each problem node
- ALWAYS include finding count by severity in header
- ALWAYS include positive points section

### MUST NOT
- NEVER review based solely on the diff — the diff is the entry point, not the scope
- NEVER approve or flag without verifying impact on callers, dependents, and integration points
- NEVER create diagram without execution flow
- NEVER omit components in the flow even if they have no problems
